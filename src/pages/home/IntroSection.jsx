/* Introduction section. Content preserved verbatim from the previous
   Home.jsx hero — not new copy. It previously lived inside the header
   markup; it's kept in the same "home-header-title-text" wrapper so it
   keeps its existing styling (max-width, spacing) unchanged. */
function IntroSection() {
  return (
    <section id="intro" className="intro-section">
      <div className="home-header-title-text">
        <p>
          Currently leading design system architecture and AI/ML UI design, I specialize in creating cohesive visual languages that make artificial intelligence accessible and empowering across complex product ecosystems.
        </p>
      </div>
    </section>
  );
}

export default IntroSection;
